[package]
name = "state"
version = "0.1.0"
edition = "2021"

[dependencies]
math = { path = "../math" }
irm-state = { path = "../irm-state" }
anchor-lang = { version = "1.0.2", default-features = false }
bytemuck = "1"

[dev-dependencies]
sha2 = "0.10"
