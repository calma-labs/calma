[package]
name = "irm-state"
version = "0.1.0"
edition = "2021"

[dependencies]
math = { path = "../math" }
interface = { path = "../interface" }
anchor-lang = { version = "1.0.2", default-features = false }
bytemuck = "1"
